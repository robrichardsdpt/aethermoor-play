{
  "name": "aethermoor",
  "version": "1.0.0",
  "description": "AETHERMOOR — The Shattered Light: an infinite procedural world-exploration game in vanilla JS. The game itself has zero dependencies; puppeteer-core only drives the headless smoke test.",
  "license": "MIT",
  "scripts": {
    "test": "node test/smoke.js && node test/mobile.js",
    "start": "python3 -m http.server 8000",
    "deploy": "git push origin main && git push play main"
  },
  "devDependencies": {
    "puppeteer-core": "^25.1.0"
  }
}
